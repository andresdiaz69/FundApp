<%= form_with(model: foundation_type, local: true) do |form| %>
  <% if foundation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(foundation_type.errors.count, "error") %> prohibited this foundation_type from being saved:</h2>

      <ul>
      <% foundation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :foundation_type_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :foundation_type_description %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.number_field :active, id: :foundation_type_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
