<%= form_with(model: profile_role, local: true) do |form| %>
  <% if profile_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile_role.errors.count, "error") %> prohibited this profile_role from being saved:</h2>

      <ul>
      <% profile_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.text_field :role_id, id: :profile_role_role_id %>
  </div>

  <div class="field">
    <%= form.label :profile_id %>
    <%= form.text_field :profile_id, id: :profile_role_profile_id %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.number_field :active, id: :profile_role_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
